# Add pinned top-level dependencies for building documentation here.
# https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html#how-to-create-reproducible-builds
# Transitive dependencies are pinned in requirements.txt, which is autogenerated with pip-compile.
# When updating dependencies here:
# - Install pip-tools with `pip install pip-tools`
# - Use `pip-compile requirements.in` to update transitive dependency versions in requirements.txt
Sphinx==7.2.6
